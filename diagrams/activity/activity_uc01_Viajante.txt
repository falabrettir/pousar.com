@startuml
hide empty description

start

:Valida formato do email;

if () then ([Email inválido])
    :Define dados como inválidos;
else ([Email válido])
    :Valida força da senha;
    
    if () then ([Senha fraca])
        :Define dados como inválidos;
    else ([Senha forte])
        :Define dados como válidos;
    endif
endif

stop

@enduml